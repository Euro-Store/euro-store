[build]
  command = "cd apps/admin && rm -rf node_modules && npm install --legacy-peer-deps --include=dev && npx next build"
  publish = "apps/admin/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV     = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"