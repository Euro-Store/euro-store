[build]
  command = "npm install && cd apps/admin && npm install styled-jsx && npx next build"
  publish = "apps/admin/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV     = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"