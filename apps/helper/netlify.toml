[build]
  base    = "apps/helper"
  command = "npm install && npx next build"
  publish = "apps/helper/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV     = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"
