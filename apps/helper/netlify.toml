[build]
  command = "npm install && cd apps/helper && npm install styled-jsx && npx next build"
  publish = "apps/helper/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV     = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"