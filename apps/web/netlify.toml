[build]
  command = "npm install && cd apps/web && npm install styled-jsx && npx next build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV     = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"